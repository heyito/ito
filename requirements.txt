# Core Application Dependencies
PyQt6==6.9.0
numpy==2.2.4
pynput==1.8.1
sounddevice==0.5.1
openai==1.72.0
pyautogui==0.9.54
PyAudio==0.2.14
soundfile==0.13.1 # Use this instead of PySoundFile
requests==2.32.3
scipy==1.15.2 # Often needed by soundfile/sounddevice
faster-whisper==1.1.1
torch==2.6.0
torchaudio==2.6.0
# torchvision==0.21.0 # Keep only if you explicitly use it for computer vision tasks
PyYAML==6.0.2 # Keep if you use YAML files
psutil==7.0.0 # Keep if you use it for system monitoring
coloredlogs==15.0.1 # Keep if you use it for logging

# macOS Integration & Related
pyobjc==11.0 # This replaces pyobjc-core and all pyobjc-framework-*
rubicon-objc==0.5.0 # Keep this specific requirement

# Build System
py2app==0.28.8
setuptools==78.1.0 # Good practice to include
wheel==0.45.1 # Good practice to include

# Dependencies (Likely pulled in automatically, but safer to list if unsure)
# --- Dependencies for Requests/OpenAI ---
certifi==2025.1.31
charset-normalizer==3.4.1
idna==3.10
urllib3==2.4.0
httpx==0.28.1
httpcore==1.0.7
anyio==4.9.0
sniffio==1.3.1
# --- Dependencies for OpenAI/Pydantic ---
pydantic==2.11.3
pydantic_core==2.33.1
annotated-types==0.7.0
typing_extensions==4.13.2
# --- Dependencies for Audio/Soundfile/Scipy ---
cffi==1.17.1
pycparser==2.22
# --- Dependencies for PyAutoGUI ---
MouseInfo==0.1.3
PyGetWindow==0.0.9
PyMsgBox==1.0.9
pyperclip==1.9.0
PyRect==0.2.0
PyScreeze==1.0.1
pytweening==1.2.0
Pillow==11.1.0 # Pillow is often needed by GUI/Image libs
# --- Dependencies for Keyboard ---
jaraco.text==4.0.0
# --- Dependencies for ML/faster-whisper/torch ---
accelerate==1.6.0 # Common ML helper lib
bitsandbytes==0.42.0 # For quantization/optimization
ctranslate2==4.6.0 # Core of faster-whisper
filelock==3.18.0 # Used by huggingface_hub/transformers
fsspec==2025.3.2 # Filesystem interactions for HF Hub
huggingface-hub==0.30.2 # For model downloading
Jinja2==3.1.6 # Templating, often used by ML libs
MarkupSafe==3.0.2 # Dep for Jinja2
networkx==3.4.2 # Often used in ML model graphs or dependencies
onnxruntime==1.21.0 # Inference runtime, likely needed by faster-whisper
packaging==24.2 # Used by many libs for version handling etc.
protobuf==6.30.2 # Data serialization, often used by ONNX/TF/etc.
regex==2024.11.6 # Improved regex lib used by tokenizers/transformers
safetensors==0.5.3 # Secure tensor storage format
sympy==1.13.1 # Symbolic math, dependency of some ML libs? Check usage.
tokenizers==0.21.1 # Core part of transformers/whisper
tqdm==4.67.1 # Progress bars
transformers==4.51.3 # Underlying library for many HF models
# --- Other Potential Dependencies ---
# Check if you directly use these or if they are pulled by less common libs
# autocommand==2.2.2
# av==14.3.0 # Audio/video encoding - check if needed by a dependency you use
# configparser==7.2.0 # Standard lib in Python 3, maybe needed for Py2 compat or specific feature?
# distro==1.9.0
# flatbuffers==25.2.10
# humanfriendly==10.0 # Often for CLI tools/logging
# jaraco.context==6.0.1
# jaraco.functools==4.1.0
# jiter==0.9.0
# mpmath==1.3.0 # Dependency for sympy
# more-itertools==10.6.0
# typing-inspection==0.4.0
# webrtcvad-wheels==2.0.14 # Voice activity detection - keep if used
# --- Build time dependencies for py2app (often installed automatically) ---
altgraph==0.17.4
macholib==1.16.3
modulegraph==0.19.6